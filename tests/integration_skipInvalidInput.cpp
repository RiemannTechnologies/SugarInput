#include <gtest/gtest.h>
#include "../lib/IOStreamReader.h"
TEST(integration, skip_invalid_input)
{
    std::stringst
    auto reader = sugar::IOStreamReader()
}
TEST(integration, skip_invalid_input_multi)
{
    
}